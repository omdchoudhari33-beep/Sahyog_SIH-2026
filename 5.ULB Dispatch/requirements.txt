fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
geoalchemy2==0.15.2
psycopg2-binary==2.9.10
pydantic==2.9.2
python-dotenv==1.0.1
pytest==8.3.3
httpx==0.27.2
# Required by FastAPI for Form()/UploadFile parsing (used by
# /status/{token}/update and /admin/onboard).
python-multipart==0.0.12
# Windows has no built-in IANA tz database - zoneinfo.ZoneInfo("Asia/Kolkata")
# in app/sla.py needs this to resolve on Windows. A no-op on Linux/macOS.
tzdata==2024.2
