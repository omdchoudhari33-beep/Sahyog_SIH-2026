DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/dno_triage

INTERNAL_SERVICE_TOKEN=

# X2 citizen status portal is intentionally read-only and public (ticket ID
# is the lookup key, not a secret token - see README). No auth needed for it.
